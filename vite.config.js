import { defineConfig } from 'vite';

export default defineConfig({
  // If your GitHub repository is https://github.com/username/resume-website,
  // change the base path to '/resume-website/'. 
  // If it's your main user site (username.github.io), set it to '/'.
  base: '/resume-website/',
});
